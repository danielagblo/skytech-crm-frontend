import api from '@/lib/axios'; import type { ApiResponse,PaginatedResponse } from '@/types/api.types'; import type { Broadcast,ContactSegment,CreateBroadcastRequest } from '@/types/broadcast.types'; export const broadcastService={getAll:()=>api.get<PaginatedResponse<Broadcast>>('/broadcasts'),getSegments:()=>api.get<ApiResponse<ContactSegment[]>>('/broadcasts/segments'),create:(data:CreateBroadcastRequest)=>api.post<ApiResponse<Broadcast>>('/broadcasts',data),send:(id:string)=>api.post<ApiResponse<Broadcast>>(`/broadcasts/${id}/send`)};
