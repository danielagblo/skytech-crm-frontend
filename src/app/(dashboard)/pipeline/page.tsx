import type { Metadata } from 'next'; import { PipelineBoard } from '@/components/pipeline/PipelineBoard'; import { PageHeader } from '@/components/shared/PageHeader'; import { Button } from '@/components/ui/button'; import { Plus } from 'lucide-react'; export const metadata:Metadata={title:'Pipeline'}; export default function PipelinePage(){return <div className="space-y-5"><PageHeader title="Pipeline" description="Move opportunities forward and capture every interaction" actions={<Button><Plus className="h-4 w-4"/>New deal</Button>}/><PipelineBoard/></div>}
