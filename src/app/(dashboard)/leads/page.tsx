import type { Metadata } from 'next'; import { TrendingUp,TrendingDown } from 'lucide-react'; import { PageHeader } from '@/components/shared/PageHeader'; import { LeadSourceStats } from '@/components/leads/LeadSourceStats'; import { LeadsTable } from '@/components/leads/LeadsTable'; export const metadata:Metadata={title:'Leads'};const stats=[['New leads added',3456,true],['Waiting leads',124,true],['Successful leads',918,true],['Lead not successful',37,false]] as const;export default function LeadsPage(){return <div className="space-y-5"><PageHeader title="Leads" description="Qualify prospects and coordinate every hand-off"/><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label,value,up])=><div key={label} className="surface p-4"><p className="eyebrow">{label}</p><div className="mt-2 flex items-end justify-between"><p className="text-2xl font-semibold">+{value}</p><span className={`flex items-center gap-1 rounded-full px-2 py-1 text-xs ${up?'bg-green-50 text-green-700':'bg-red-50 text-red-700'}`}>{up?<TrendingUp className="h-3 w-3"/>:<TrendingDown className="h-3 w-3"/>}23%</span></div></div>)}</div><LeadSourceStats/><LeadsTable/></div>}
