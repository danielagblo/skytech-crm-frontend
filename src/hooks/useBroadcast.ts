'use client'; import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query'; import { toast } from 'sonner'; import { broadcastService } from '@/services/broadcast.service'; export const useBroadcasts=()=>useQuery({queryKey:['broadcasts'],queryFn:broadcastService.getAll}); export const useContactSegments=()=>useQuery({queryKey:['broadcast-segments'],queryFn:broadcastService.getSegments}); export const useCreateBroadcast=()=>{const client=useQueryClient();return useMutation({mutationFn:broadcastService.create,onSuccess:()=>{void client.invalidateQueries({queryKey:['broadcasts']});toast.success('Broadcast saved')},onError:()=>toast.error('Failed to save broadcast')})};
