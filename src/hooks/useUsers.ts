'use client'; import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query'; import { toast } from 'sonner'; import { usersService } from '@/services/users.service'; export const useUsers=()=>useQuery({queryKey:['users'],queryFn:usersService.getAll}); export const useDeleteUser=()=>{const client=useQueryClient();return useMutation({mutationFn:usersService.delete,onSuccess:()=>{void client.invalidateQueries({queryKey:['users']});toast.success('Agent removed')},onError:()=>toast.error('Could not remove agent')})};
