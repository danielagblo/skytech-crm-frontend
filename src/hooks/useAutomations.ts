'use client'; import { useMutation,useQuery,useQueryClient } from '@tanstack/react-query'; import { toast } from 'sonner'; import { automationsService } from '@/services/automations.service'; export const useAutomations=()=>useQuery({queryKey:['automations'],queryFn:automationsService.getAll}); export const useToggleAutomation=()=>{const client=useQueryClient();return useMutation({mutationFn:({id,enabled}:{id:string;enabled:boolean})=>automationsService.toggle(id,enabled),onSuccess:()=>void client.invalidateQueries({queryKey:['automations']}),onError:()=>toast.error('Could not update automation')})};
