'use client';
import { useRef,useState } from 'react';
import { useMutation } from '@tanstack/react-query';
import { z } from 'zod';
import { useVerifyOtp } from '@/hooks/useAuth';
import { authService } from '@/services/auth.service';
import type { LoginRequest } from '@/types/auth.types';
import { Button } from '@/components/ui/button';
import { toast } from 'sonner';
const schema=z.string().regex(/^\d{4}$/,'Enter the 4-digit code');
export const OtpForm=()=>{const [digits,setDigits]=useState(['','','','']);const refs=useRef<Array<HTMLInputElement|null>>([]);const verify=useVerifyOtp();const resend=useMutation({mutationFn:(data:LoginRequest)=>authService.login(data),onSuccess:()=>toast.success('A new code has been sent'),onError:()=>toast.error('Could not resend the code')});const change=(value:string,index:number)=>{const digit=value.replace(/\D/g,'').slice(-1);setDigits((current)=>current.map((v,i)=>i===index?digit:v));if(digit&&index<3)refs.current[index+1]?.focus()};const submit=()=>{const otp=digits.join('');const parsed=schema.safeParse(otp);if(!parsed.success){toast.error(parsed.error.issues[0].message);return}const userId=sessionStorage.getItem('skytech_user_id');if(!userId){toast.error('Your verification session expired. Please log in again.');return}verify.mutate({userId,otp})};const resendCode=()=>{const saved=sessionStorage.getItem('skytech_login_attempt');if(!saved){toast.error('Return to login to request a new code');return}try{resend.mutate(JSON.parse(saved) as LoginRequest)}catch{toast.error('Return to login to request a new code')}};return <div className="space-y-6"><div className="flex justify-center gap-3">{digits.map((digit,index)=><input key={index} ref={(el)=>{refs.current[index]=el}} aria-label={`OTP digit ${index+1}`} inputMode="numeric" maxLength={1} value={digit} onChange={(e)=>change(e.target.value,index)} onKeyDown={(e)=>{if(e.key==='Backspace'&&!digits[index]&&index>0)refs.current[index-1]?.focus()}} className="h-14 w-14 rounded-xl border bg-white text-center text-2xl font-semibold shadow-sm focus:border-primary focus:ring-2 focus:ring-primary/30"/>)}</div><p className="text-center text-sm text-muted-foreground">Did not get a code? <button className="font-semibold text-green-700 disabled:opacity-50" disabled={resend.isPending} onClick={resendCode}>{resend.isPending?'Sending…':'Resend'}</button></p><Button className="w-full" onClick={submit} disabled={verify.isPending}>{verify.isPending?'Verifying…':'Verify & continue'}</Button></div>};
