import { ArrowRight } from 'lucide-react'; import type { Lead } from '@/types/lead.types'; import { TableCell,TableRow } from '@/components/ui/table'; import { AssigneeStack } from '@/components/shared/AssigneeStack'; import { PriorityBadge } from '@/components/shared/PriorityBadge'; export const LeadRow=({lead,onOpen}:{lead:Lead;onOpen:(lead:Lead)=>void})=><TableRow><TableCell className="font-semibold">{lead.firstName} {lead.lastName}</TableCell><TableCell>{lead.phone}</TableCell><TableCell>{lead.company}</TableCell><TableCell>{lead.location}</TableCell><TableCell>{lead.role}</TableCell><TableCell className="max-w-36 truncate">{lead.email}</TableCell><TableCell><AssigneeStack users={lead.assignees}/></TableCell><TableCell>{lead.category}</TableCell><TableCell><div className="flex items-center gap-2"><span className="h-1.5 w-16 overflow-hidden rounded-full bg-gray-100"><span className="block h-full bg-primary" style={{width:`${lead.conversion}%`}}/></span>{lead.conversion}%</div></TableCell><TableCell><div className="flex gap-1">{lead.sources.map((s)=><span key={s} className="rounded bg-muted px-1.5 py-1 text-[10px]">{s.replace('_',' ')}</span>)}</div></TableCell><TableCell><PriorityBadge priority={lead.priority}/></TableCell><TableCell><button onClick={()=>onOpen(lead)} className="rounded-full p-2 hover:bg-muted" aria-label={`Open ${lead.firstName}`}><ArrowRight className="h-4 w-4"/></button></TableCell></TableRow>;
