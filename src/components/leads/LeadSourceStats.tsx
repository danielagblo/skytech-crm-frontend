import type { LeadSource } from '@/types/api.types';
const labels: Record<LeadSource, string> = { SMS: 'SMS', EMAIL: 'Email', FACEBOOK: 'Facebook', GOOGLE: 'Google', BANNER: 'Banner', META_ADS: 'Meta Ads' };
export const LeadSourceStats = ({ breakdown, total }: { breakdown: Partial<Record<LeadSource, number>>; total: number }) => <div className="surface flex flex-wrap items-center gap-2 rounded-xl p-3"><p className="eyebrow mr-2">Lead source</p>{Object.entries(labels).map(([source, label]) => { const count = breakdown[source as LeadSource] ?? 0; const percentage = total > 0 ? Math.round(count * 100 / total) : 0; return <span key={source} className="rounded-full border bg-white px-3 py-1.5 text-xs"><strong>{label}</strong> <span className="text-green-700">{percentage}%</span></span>; })}</div>;
