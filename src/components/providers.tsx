'use client'; import { useEffect } from 'react'; import { QueryClientProvider } from '@tanstack/react-query'; import { ThemeProvider } from 'next-themes'; import { Toaster } from 'sonner'; import { queryClient } from '@/lib/queryClient'; import { useAuthStore } from '@/store/authStore'; export const Providers=({children}:{children:React.ReactNode})=>{const rehydrate=useAuthStore((s)=>s.rehydrate);useEffect(()=>rehydrate(),[rehydrate]);return <QueryClientProvider client={queryClient}><ThemeProvider attribute="class" defaultTheme="light" enableSystem><Toaster richColors position="top-right"/>{children}</ThemeProvider></QueryClientProvider>};
