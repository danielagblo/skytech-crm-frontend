'use client';
import { useState } from 'react';
import { DragDropContext,type DropResult } from '@hello-pangea/dnd';
import { Plus,Search,TrendingUp } from 'lucide-react';
import type { Task } from '@/types/task.types';
import type { Priority,TaskStatus } from '@/types/api.types';
import { tasks as initialTasks,people } from '@/lib/mock-data';
import { useUpdateTaskStatus } from '@/hooks/useTasks';
import { TaskColumn } from './TaskColumn';
import { TaskDetail } from './TaskDetail';
import { CreateTaskModal } from './CreateTaskModal';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
const statuses:TaskStatus[]=['TODO','OVERDUE','DOING','DONE'];
export const TaskBoard=()=>{const [items,setItems]=useState(initialTasks);const [selected,setSelected]=useState<Task|null>(null);const [create,setCreate]=useState(false);const [search,setSearch]=useState('');const [priority,setPriority]=useState<Priority>();const [assignee,setAssignee]=useState<string>();const updateStatus=useUpdateTaskStatus();const filtered=items.filter((task)=>task.title.toLowerCase().includes(search.toLowerCase())&&(!priority||task.priority===priority)&&(!assignee||task.assignees.some((user)=>user.id===assignee)));const drop=(result:DropResult)=>{if(!result.destination)return;const status=result.destination.droppableId as TaskStatus;if(result.source.droppableId===status&&result.source.index===result.destination.index)return;setItems((list)=>list.map((task)=>task.id===result.draggableId?{...task,status}:task));updateStatus.mutate({id:result.draggableId,status})};return <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-3">{[['Total Tasks',items.length],['Total Task Done',items.filter((task)=>task.status==='DONE').length],['Overdue',items.filter((task)=>task.status==='OVERDUE').length]].map(([label,value])=><div key={String(label)} className="surface flex items-center justify-between p-4"><div><p className="eyebrow">{label}</p><p className="mt-1 text-2xl font-semibold">{value}</p></div><span className="flex items-center gap-1 text-xs text-green-700"><TrendingUp className="h-4 w-4"/>12%</span></div>)}</div><div className="surface flex flex-wrap items-center gap-2 rounded-xl p-3"><div className="relative min-w-56 flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input value={search} onChange={(event)=>setSearch(event.target.value)} placeholder="Search tasks" className="pl-9"/></div><Select onValueChange={(value)=>setAssignee(value==='ALL'?undefined:value)}><SelectTrigger className="w-44"><SelectValue placeholder="Assignee"/></SelectTrigger><SelectContent><SelectItem value="ALL">All assignees</SelectItem>{people.map((person)=><SelectItem key={person.id} value={person.id}>{person.firstName} {person.lastName}</SelectItem>)}</SelectContent></Select><Select onValueChange={(value)=>setPriority(value==='ALL'?undefined:value as Priority)}><SelectTrigger className="w-40"><SelectValue placeholder="Priority"/></SelectTrigger><SelectContent><SelectItem value="ALL">All priorities</SelectItem><SelectItem value="LOW">Low</SelectItem><SelectItem value="MEDIUM">Medium</SelectItem><SelectItem value="HIGH">High</SelectItem></SelectContent></Select><Button onClick={()=>setCreate(true)}><Plus className="h-4 w-4"/>Create task</Button></div><DragDropContext onDragEnd={drop}><div className="dot-grid flex min-h-[600px] gap-4 overflow-x-auto rounded-2xl border p-4">{statuses.map((status)=><TaskColumn key={status} status={status} tasks={filtered.filter((task)=>task.status===status)} onOpen={setSelected}/>)}</div></DragDropContext><TaskDetail task={selected} open={Boolean(selected)} onOpenChange={(value)=>!value&&setSelected(null)}/><CreateTaskModal open={create} onOpenChange={setCreate}/></div>};
