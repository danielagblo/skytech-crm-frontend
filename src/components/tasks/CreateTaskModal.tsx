'use client';
import { useState } from 'react';
import { useFieldArray,useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import { Plus,Trash2 } from 'lucide-react';
import { Dialog,DialogContent,DialogHeader,DialogTitle } from '@/components/ui/dialog';
import { Tabs,TabsContent,TabsList,TabsTrigger } from '@/components/ui/tabs';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Textarea } from '@/components/ui/textarea';
import { Label } from '@/components/ui/label';
import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';
import { leads } from '@/lib/mock-data';
import { toast } from 'sonner';
const schema=z.object({title:z.string().min(3),priority:z.enum(['LOW','MEDIUM','HIGH']),createdBy:z.string().min(1),reminder:z.boolean(),description:z.string().min(10),subTasks:z.array(z.object({title:z.string().min(2),priority:z.enum(['LOW','MEDIUM','HIGH']),description:z.string().min(3)}))});
type Values=z.infer<typeof schema>;
export const CreateTaskModal=({open,onOpenChange}:{open:boolean;onOpenChange:(value:boolean)=>void})=>{const [caseId,setCaseId]=useState('');const {register,control,setValue,handleSubmit,formState:{errors,isSubmitting}}=useForm<Values>({resolver:zodResolver(schema),mode:'onBlur',defaultValues:{priority:'LOW',createdBy:'u1',reminder:true,description:'',subTasks:[]}});const fields=useFieldArray({control,name:'subTasks'});const save=handleSubmit(()=>{toast.success('Task created');onOpenChange(false)});return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-2xl"><DialogHeader><DialogTitle>Create task</DialogTitle></DialogHeader><Tabs defaultValue="manual"><TabsList><TabsTrigger value="manual">Manual entry</TabsTrigger><TabsTrigger value="case">Select pending case</TabsTrigger></TabsList><TabsContent value="manual"><form className="space-y-4" onSubmit={save}><div><Label>Title</Label><Input {...register('title')}/>{errors.title&&<p className="text-xs text-danger">Enter a task title</p>}</div><div className="grid gap-3 sm:grid-cols-2"><div><Label>Priority</Label><Select defaultValue="LOW" onValueChange={(value:'LOW'|'MEDIUM'|'HIGH')=>setValue('priority',value)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="LOW">Low</SelectItem><SelectItem value="MEDIUM">Medium</SelectItem><SelectItem value="HIGH">High</SelectItem></SelectContent></Select></div><div><Label>Created by</Label><Select defaultValue="u1" onValueChange={(value)=>setValue('createdBy',value)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="u1">Jeffrey Henadez</SelectItem><SelectItem value="u2">John Alan</SelectItem></SelectContent></Select></div></div><div className="flex items-center justify-between rounded-lg border p-3"><Label>Allow reminder</Label><Switch defaultChecked onCheckedChange={(value)=>setValue('reminder',value)}/></div><div><Label>Description</Label><Textarea {...register('description')}/>{errors.description&&<p className="text-xs text-danger">Add at least 10 characters</p>}</div><div><div className="mb-2 flex items-center justify-between"><Label>Sub tasks</Label><Button type="button" variant="outline" size="sm" onClick={()=>fields.append({title:'',priority:'LOW',description:''})}><Plus className="h-3 w-3"/>Add subtask</Button></div>{fields.fields.map((field,index)=><div key={field.id} className="mb-3 grid gap-2 rounded-xl border p-3"><div className="flex gap-2"><Input placeholder="Subtask title" {...register(`subTasks.${index}.title`)}/><Button type="button" size="icon" variant="ghost" onClick={()=>fields.remove(index)}><Trash2 className="h-4 w-4"/></Button></div><Select defaultValue="LOW" onValueChange={(value:'LOW'|'MEDIUM'|'HIGH')=>setValue(`subTasks.${index}.priority`,value)}><SelectTrigger><SelectValue placeholder="Subtask priority"/></SelectTrigger><SelectContent><SelectItem value="LOW">Low priority</SelectItem><SelectItem value="MEDIUM">Medium priority</SelectItem><SelectItem value="HIGH">High priority</SelectItem></SelectContent></Select><Textarea placeholder="Description" {...register(`subTasks.${index}.description`)}/></div>)}</div><Button className="w-full" disabled={isSubmitting}>Confirm</Button></form></TabsContent><TabsContent value="case"><p className="mb-3 text-sm text-muted-foreground">Select any pending lead or deal to auto-populate the task.</p><div className="space-y-2">{leads.slice(0,4).map((lead)=><button key={lead.id} onClick={()=>setCaseId(lead.id)} className={`w-full rounded-xl border p-3 text-left ${caseId===lead.id?'border-primary bg-green-50':''}`}><strong className="text-sm">{lead.firstName} {lead.lastName}</strong><p className="text-xs text-muted-foreground">{lead.company} · {lead.category}</p></button>)}</div><Button className="mt-4 w-full" onClick={()=>{toast.success('Task created from case');onOpenChange(false)}} disabled={!caseId}>Create from selected case</Button></TabsContent></Tabs></DialogContent></Dialog>};
