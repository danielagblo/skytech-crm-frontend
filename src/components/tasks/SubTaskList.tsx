'use client'; import { useState } from 'react'; import type { SubTask } from '@/types/task.types'; import { Checkbox } from '@/components/ui/checkbox'; import { PriorityBadge } from '@/components/shared/PriorityBadge'; export const SubTaskList=({items}:{items:SubTask[]})=>{const [done,setDone]=useState<Record<string,boolean>>(Object.fromEntries(items.map((i)=>[i.id,i.done])));return <div className="space-y-2">{items.map((item)=><div key={item.id} className="rounded-xl border p-3"><div className="flex items-start gap-3"><Checkbox checked={done[item.id]} onCheckedChange={(v)=>setDone((s)=>({...s,[item.id]:Boolean(v)}))}/><div className="flex-1"><div className="flex justify-between gap-2"><p className={`text-sm font-semibold ${done[item.id]?'line-through text-muted-foreground':''}`}>{item.title}</p><PriorityBadge priority={item.priority}/></div><p className="mt-1 text-xs text-muted-foreground">{item.description}</p></div></div></div>)}</div>};
