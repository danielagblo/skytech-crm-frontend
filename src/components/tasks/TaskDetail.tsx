'use client';
import { useState } from 'react';
import { Bell, Plus, UserRound } from 'lucide-react';
import type { Task } from '@/types/task.types';
import type { User } from '@/types/user.types';
import { useAddTaskComment, useCreateSubtask, useTaskComments, useTaskSubtasks, useUpdateTask } from '@/hooks/useTasks';
import { Sheet, SheetContent, SheetHeader, SheetTitle } from '@/components/ui/sheet';
import { StatusBadge } from '@/components/shared/StatusBadge';
import { PriorityBadge } from '@/components/shared/PriorityBadge';
import { Switch } from '@/components/ui/switch';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { SubTaskList } from './SubTaskList';
import { CommentThread } from '@/components/shared/CommentThread';
export const TaskDetail = ({ task, users, open, onOpenChange }: { task: Task | null; users: User[]; open: boolean; onOpenChange: (value: boolean) => void }) => { const [adding, setAdding] = useState(false); const [title, setTitle] = useState(''); const subtasks = useTaskSubtasks(task?.id ?? ''); const comments = useTaskComments(task?.id ?? ''); const createSubtask = useCreateSubtask(); const addComment = useAddTaskComment(); const update = useUpdateTask(); if (!task) return null; const creator = users.find((user) => user.id === task.createdById); const add = () => { if (!title.trim()) return; createSubtask.mutate({ taskId: task.id, data: { title: title.trim(), priority: 'LOW', complete: false } }, { onSuccess: () => { setTitle(''); setAdding(false); } }); }; return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent className="max-w-lg"><SheetHeader><SheetTitle>{task.title}</SheetTitle><div className="flex gap-2"><StatusBadge status={task.status} />{task.priority && <PriorityBadge priority={task.priority} />}</div></SheetHeader><div className="space-y-6"><div className="grid gap-3 rounded-xl bg-muted p-4 text-sm"><p className="flex items-center gap-2"><UserRound className="h-4 w-4" />Created by <strong>{creator ? `${creator.firstName} ${creator.lastName}` : 'CRM user'}</strong></p><div className="flex items-center justify-between"><span className="flex items-center gap-2"><Bell className="h-4 w-4" />Allow reminder</span><Switch checked={task.allowReminder} disabled={update.isPending} onCheckedChange={(value) => update.mutate({ id: task.id, data: { title: task.title, allowReminder: value, version: task.version } })} /></div></div><section><p className="eyebrow mb-2">Description</p><p className="text-sm leading-relaxed text-muted-foreground">{task.description || 'No description provided.'}</p></section><section><div className="mb-3 flex items-center justify-between"><h3 className="font-semibold">Sub tasks · {subtasks.data?.length ?? 0}</h3><Button size="sm" variant="outline" onClick={() => setAdding((value) => !value)}><Plus className="h-3.5 w-3.5" />Add subtask</Button></div>{adding && <div className="mb-3 flex gap-2"><Input value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Subtask title" /><Button size="sm" disabled={!title.trim() || createSubtask.isPending} onClick={add}>Add</Button></div>}<SubTaskList taskId={task.id} items={subtasks.data ?? []} /></section><section><h3 className="mb-3 font-semibold">Comments</h3><CommentThread comments={comments.data ?? []} onAdd={(body) => addComment.mutate({ taskId: task.id, body })} pending={addComment.isPending} /></section></div></SheetContent></Sheet>; };
