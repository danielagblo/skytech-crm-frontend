'use client'; import { Bell,Plus,UserRound } from 'lucide-react'; import type { Task } from '@/types/task.types'; import { Sheet,SheetContent,SheetHeader,SheetTitle } from '@/components/ui/sheet'; import { StatusBadge } from '@/components/shared/StatusBadge'; import { PriorityBadge } from '@/components/shared/PriorityBadge'; import { Switch } from '@/components/ui/switch'; import { Button } from '@/components/ui/button'; import { SubTaskList } from './SubTaskList'; import { CommentThread } from '@/components/shared/CommentThread'; export const TaskDetail=({task,open,onOpenChange}:{task:Task|null;open:boolean;onOpenChange:(v:boolean)=>void})=>{if(!task)return null;return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent className="max-w-lg"><SheetHeader><SheetTitle>{task.title}</SheetTitle><div className="flex gap-2"><StatusBadge status={task.status}/><PriorityBadge priority={task.priority}/></div></SheetHeader><div className="space-y-6"><div className="grid gap-3 rounded-xl bg-muted p-4 text-sm"><p className="flex items-center gap-2"><UserRound className="h-4 w-4"/>Created by <strong>{task.createdBy.firstName} {task.createdBy.lastName}</strong></p><div className="flex items-center justify-between"><span className="flex items-center gap-2"><Bell className="h-4 w-4"/>Allow reminder</span><Switch defaultChecked={task.reminder}/></div></div><section><p className="eyebrow mb-2">Description</p><p className="text-sm leading-relaxed text-muted-foreground">{task.description}</p></section><section><div className="mb-3 flex items-center justify-between"><h3 className="font-semibold">Sub tasks · {task.subTasks.length}</h3><Button size="sm" variant="outline"><Plus className="h-3.5 w-3.5"/>Add subtask</Button></div><SubTaskList items={task.subTasks}/></section><section><h3 className="mb-3 font-semibold">Comments</h3><CommentThread comments={task.comments}/></section></div></SheetContent></Sheet>};
