import { Calendar,MessageSquare,Network } from 'lucide-react'; import type { Task } from '@/types/task.types'; import { PriorityBadge } from '@/components/shared/PriorityBadge'; import { AssigneeStack } from '@/components/shared/AssigneeStack'; import { formatDate } from '@/lib/utils'; export const TaskCard=({task,onClick}:{task:Task;onClick:()=>void})=><button onClick={onClick} className="w-full rounded-xl border bg-white p-3 text-left shadow-sm transition hover:shadow-md"><h4 className="line-clamp-2 text-sm font-semibold">{task.title}</h4><div className="mt-3 flex items-center justify-between"><PriorityBadge priority={task.priority}/><AssigneeStack users={task.assignees}/></div><p className="mt-3 flex items-center gap-1 text-xs text-muted-foreground"><Calendar className="h-3.5 w-3.5"/>Due {formatDate(task.dueAt)}</p><div className="mt-3 flex gap-3 border-t pt-2 text-[10px] text-muted-foreground"><span className="flex items-center gap-1"><Network className="h-3 w-3"/>{task.subTasks.length} subtasks</span><span className="flex items-center gap-1"><MessageSquare className="h-3 w-3"/>{task.comments.length} comments</span></div></button>;
