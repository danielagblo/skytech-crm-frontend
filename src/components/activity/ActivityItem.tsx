import { Activity as ActivityIcon,MessageSquare,PhoneCall,Target,UserPlus } from 'lucide-react'; import type { Activity } from '@/types/activity.types'; import { formatRelative } from '@/lib/utils'; const icons={CALL:PhoneCall,COMMENT:MessageSquare,LEAD:UserPlus,TASK:Target,SECURITY:ActivityIcon}; export const ActivityItem=({activity}:{activity:Activity})=>{const Icon=icons[activity.type];return <div className="flex min-w-72 items-center gap-3 rounded-xl border bg-white p-3"><span className="rounded-full bg-green-50 p-2"><Icon className="h-4 w-4 text-green-700"/></span><div className="min-w-0"><p className="truncate text-sm font-medium">{activity.title}</p><p className="truncate text-xs text-muted-foreground">{activity.description}</p><p className="mt-1 text-[10px] text-gray-400">{formatRelative(activity.createdAt)}</p></div></div>};
