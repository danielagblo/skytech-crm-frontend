import { activities } from '@/lib/mock-data'; import { ActivityItem } from './ActivityItem'; export const ActivityLog=()=> <section><div className="mb-3 flex items-center justify-between"><h3 className="font-semibold">Recent activity</h3><span className="text-xs text-muted-foreground">Live team feed</span></div><div className="scrollbar-thin flex gap-3 overflow-x-auto pb-2">{activities.map((activity)=><ActivityItem key={activity.id} activity={activity}/>)}</div></section>;
