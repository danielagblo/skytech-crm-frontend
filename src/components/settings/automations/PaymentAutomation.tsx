import { GitBranch,Mail,MessageSquare,Pause,SkipForward } from 'lucide-react'; const branches:[string,string[]][]=[['Missed payment',['Wait 2 days','Hold on until due','Send payment reminder']],['Service recovery',['Wait 1 month','Hold on until renewal','Offer service recovery']],['Payment received',['Wait 1 day','Confirm receipt','Send thank-you message']]];export const PaymentAutomation=()=> <section className="space-y-5"><div><h2 className="text-lg font-semibold">Payment automation</h2><p className="text-sm text-muted-foreground">Read-only workflow preview for payment lifecycle communication.</p></div><div className="mx-auto max-w-sm rounded-xl border-2 border-primary bg-green-50 p-4 text-center"><p className="eyebrow">Trigger</p><strong>Payment received or waiting for payment</strong></div><div className="mx-auto h-8 w-px bg-gray-300"/><div className="flex items-center justify-center gap-2 text-xs text-muted-foreground"><GitBranch className="h-4 w-4"/>Split into 3 paths</div><div className="grid gap-5 lg:grid-cols-3">{branches.map(([name,steps],bi)=><div key={name} className="surface p-4"><div className="mb-4 flex items-center gap-2"><span className="flex h-7 w-7 items-center justify-center rounded-full bg-gray-900 text-xs text-white">{bi+1}</span><h3 className="font-semibold">{name}</h3></div><div className="space-y-0">{steps.map((step,i)=><div key={step}><div className="rounded-xl border bg-white p-3"><p className="text-[10px] uppercase text-muted-foreground">Step {i+1}</p><p className="text-sm font-medium">{step}</p><div className="mt-2 flex gap-1">{[Pause,MessageSquare,Mail,SkipForward].map((Icon,j)=><button key={j} className="rounded border p-1"><Icon className="h-3 w-3"/></button>)}</div></div>{i<2&&<div className="mx-auto h-5 w-px bg-primary"/>}</div>)}</div></div>)}</div></section>;
