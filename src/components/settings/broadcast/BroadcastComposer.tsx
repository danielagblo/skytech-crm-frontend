'use client';
import { useState } from 'react';
import { useForm, useWatch } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import { CalendarClock, Send } from 'lucide-react';
import { toast } from 'sonner';
import { useCreateBroadcast, useScheduleBroadcast, useSendBroadcast } from '@/hooks/useBroadcast';
import { ContactSegmentSelector } from './ContactSegmentSelector';
import { Textarea } from '@/components/ui/textarea';
import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import type { BroadcastChannel } from '@/types/api.types';
const schema = z.object({ name: z.string().min(3, 'Name this broadcast.'), message: z.string().min(1, 'Write a message.').max(160), channel: z.enum(['SMS', 'EMAIL']), scheduledAt: z.string().optional() });
type Values = z.infer<typeof schema>;
export const BroadcastComposer = () => { const [segments, setSegments] = useState<string[]>([]); const create = useCreateBroadcast(); const send = useSendBroadcast(); const schedule = useScheduleBroadcast(); const { register, control, setValue, reset, handleSubmit, formState: { errors } } = useForm<Values>({ resolver: zodResolver(schema), mode: 'onBlur', defaultValues: { name: '', message: '', channel: 'SMS', scheduledAt: '' } }); const values = useWatch({ control }); const process = (scheduled: boolean) => handleSubmit(async (data) => { if (!segments.length) { toast.error('Select a recipient segment before continuing.'); return; } if (scheduled && (!data.scheduledAt || new Date(data.scheduledAt) <= new Date())) { toast.error('Choose a future date and time for the scheduled broadcast.'); return; } const segmentFilter = segments[0] === 'all' ? { all: true } : { stage: segments[0] }; try { const created = await create.mutateAsync({ name: data.name, messageContent: data.message, channel: data.channel as BroadcastChannel, segmentFilter }); if (scheduled && data.scheduledAt) await schedule.mutateAsync({ id: created.data.data.id, scheduledAt: new Date(data.scheduledAt).toISOString() }); else await send.mutateAsync(created.data.data.id); reset(); setSegments([]); } catch { /* Mutation hooks present the backend's actionable error. */ } })(); const pending = create.isPending || send.isPending || schedule.isPending; return <div className="grid gap-5 lg:grid-cols-[280px_1fr]"><ContactSegmentSelector selected={segments} onChange={setSegments} /><section className="surface p-5"><div className="mb-5"><h2 className="text-lg font-semibold">Message composer</h2><p className="text-sm text-muted-foreground">Only opted-in contacts with a valid destination receive a broadcast.</p></div><form className="space-y-4"><div><Label>Broadcast name</Label><Input placeholder="e.g. August payment reminder" {...register('name')} />{errors.name && <p className="text-xs text-danger">{errors.name.message}</p>}</div><div><Label>Channel</Label><Select value={values.channel} onValueChange={(value: BroadcastChannel) => setValue('channel', value)}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="SMS">SMS</SelectItem><SelectItem value="EMAIL">Email</SelectItem></SelectContent></Select></div><div><div className="mb-2 flex items-center justify-between"><Label>{values.channel} message</Label><span className="text-xs text-muted-foreground">{values.message?.length ?? 0}/160 · 1 SMS credit</span></div><Textarea className="min-h-52" placeholder="Write your message…" maxLength={160} {...register('message')} />{errors.message && <p className="text-xs text-danger">{errors.message.message}</p>}</div><div><Label>Schedule date and time</Label><Input type="datetime-local" {...register('scheduledAt')} /><p className="mt-1 text-xs text-muted-foreground">Required only when you choose Schedule.</p></div><div className="flex justify-end gap-2"><Button type="button" variant="outline" disabled={pending} onClick={() => void process(true)}><CalendarClock className="h-4 w-4" />{schedule.isPending ? 'Scheduling…' : 'Schedule'}</Button><Button type="button" disabled={pending} onClick={() => void process(false)}><Send className="h-4 w-4" />{send.isPending ? 'Sending…' : 'Send now'}</Button></div></form></section></div>; };
