'use client'; import { Checkbox } from '@/components/ui/checkbox'; const segments=[['all','All leads',3456],['negotiation','Negotiations',128],['settlement','Settlement',64],['payment','Payment',42]] as const;export const ContactSegmentSelector=({selected,onChange}:{selected:string[];onChange:(ids:string[])=>void})=><aside className="surface h-fit overflow-hidden"><div className="border-b p-4"><h3 className="font-semibold">Contact segments</h3><p className="text-xs text-muted-foreground">Choose broadcast recipients</p></div><div className="divide-y">{segments.map(([id,name,count])=><label key={id} className="flex cursor-pointer items-center gap-3 p-4 hover:bg-muted/50"><Checkbox checked={selected.includes(id)} onCheckedChange={(v)=>onChange(v?[...selected,id]:selected.filter((s)=>s!==id))}/><span className="flex-1 text-sm font-medium">{name}</span><span className="rounded-full bg-muted px-2 py-1 text-xs">{count.toLocaleString()}</span></label>)}</div></aside>;
