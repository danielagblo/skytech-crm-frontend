'use client'; import { useEffect,useState } from 'react'; import { useForm } from 'react-hook-form'; import { z } from 'zod'; import { zodResolver } from '@hookform/resolvers/zod'; import type { User } from '@/types/user.types'; import { Camera,Trash2 } from 'lucide-react'; import { Sheet,SheetContent,SheetHeader,SheetTitle } from '@/components/ui/sheet'; import { Button } from '@/components/ui/button'; import { Input } from '@/components/ui/input'; import { Label } from '@/components/ui/label'; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from '@/components/ui/select'; import { UserAvatar } from '@/components/shared/UserAvatar'; import { ConfirmModal } from '@/components/shared/ConfirmModal'; import { usePermission } from '@/hooks/usePermission'; import { toast } from 'sonner'; const schema=z.object({firstName:z.string().min(2),lastName:z.string().min(2),email:z.string().email(),role:z.enum(['ADMIN','MANAGER','AGENT']),phone:z.string().min(8),password:z.string().min(8),username:z.string().min(3)});type Values=z.infer<typeof schema>;export const AddAgentModal=({agent,open,onOpenChange}:{agent?:User|null;open:boolean;onOpenChange:(v:boolean)=>void})=>{const [confirm,setConfirm]=useState(false);const {can}=usePermission();const {register,setValue,reset,handleSubmit,formState:{errors}}=useForm<Values>({resolver:zodResolver(schema),mode:'onBlur',defaultValues:{role:'AGENT'}});useEffect(()=>{if(agent)reset({firstName:agent.firstName,lastName:agent.lastName,email:agent.email,role:agent.role,phone:agent.phone||'',password:'••••••••',username:`${agent.firstName}.${agent.lastName}`.toLowerCase()})},[agent,reset]);return <><Sheet open={open} onOpenChange={onOpenChange}><SheetContent><SheetHeader><SheetTitle>{agent?'Agent details':'Add agent'}</SheetTitle></SheetHeader><form className="space-y-4" onSubmit={handleSubmit(()=>{toast.success(agent?'Agent updated':'Agent added');onOpenChange(false)})}><div className="flex flex-col items-center"><UserAvatar name={agent?`${agent.firstName} ${agent.lastName}`:'New Agent'} className="h-20 w-20"/><Button type="button" size="sm" variant="outline" className="mt-2"><Camera className="h-4 w-4"/>Edit photo</Button></div><div className="grid grid-cols-2 gap-3"><div><Label>First name</Label><Input {...register('firstName')}/></div><div><Label>Last name</Label><Input {...register('lastName')}/></div></div><div><Label>Email</Label><Input type="email" {...register('email')}/>{errors.email&&<p className="text-xs text-danger">Enter a valid email</p>}</div><div><Label>Role</Label><Select defaultValue={agent?.role||'AGENT'} onValueChange={(v:'ADMIN'|'MANAGER'|'AGENT')=>setValue('role',v)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="ADMIN">Admin</SelectItem><SelectItem value="MANAGER">Manager</SelectItem><SelectItem value="AGENT">Agent</SelectItem></SelectContent></Select></div><div><Label>Number</Label><Input {...register('phone')}/></div><div><Label>Username</Label><Input {...register('username')}/></div><div><Label>Password</Label><Input type="password" {...register('password')}/></div><Button className="w-full">Save agent</Button>{agent&&can('manage:users')&&<Button type="button" variant="destructive" className="w-full" onClick={()=>setConfirm(true)}><Trash2 className="h-4 w-4"/>Remove user</Button>}</form></SheetContent></Sheet><ConfirmModal open={confirm} onOpenChange={setConfirm} title="Remove this agent?" description="The agent will lose access immediately. Their records will remain in the CRM." confirmLabel="Remove agent" onConfirm={()=>{setConfirm(false);onOpenChange(false)}}/></>};
