import type { User } from '@/types/user.types'; import { UserAvatar } from '@/components/shared/UserAvatar'; import { formatCurrency } from '@/lib/utils'; export const AgentCard=({agent}:{agent:User})=><div className="surface p-4"><div className="flex items-center gap-3"><UserAvatar name={`${agent.firstName} ${agent.lastName}`} className="h-11 w-11"/><div><p className="font-semibold">{agent.firstName} {agent.lastName}</p><p className="text-xs text-muted-foreground">{agent.role}</p></div></div><div className="mt-4 grid grid-cols-2 gap-2 text-sm"><div className="rounded-lg bg-muted p-2"><p className="eyebrow">Revenue</p><strong>{formatCurrency(agent.revenue||0)}</strong></div><div className="rounded-lg bg-muted p-2"><p className="eyebrow">Hours</p><strong>{agent.hoursSpent}</strong></div></div></div>;
