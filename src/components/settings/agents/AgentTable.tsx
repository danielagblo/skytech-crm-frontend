import type { User } from '@/types/user.types'; import { formatCurrency,formatDate } from '@/lib/utils'; import { UserAvatar } from '@/components/shared/UserAvatar'; import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from '@/components/ui/table'; export const AgentTable=({agents,onOpen}:{agents:User[];onOpen:(agent:User)=>void})=><div className="surface overflow-hidden"><Table><TableHeader><TableRow><TableHead>Name</TableHead><TableHead>Role</TableHead><TableHead>Status</TableHead><TableHead>Revenue</TableHead><TableHead>Hours spent</TableHead><TableHead>Date added</TableHead></TableRow></TableHeader><TableBody>{agents.map((agent)=><TableRow key={agent.id} onClick={()=>onOpen(agent)} className="cursor-pointer"><TableCell><div className="flex items-center gap-2"><UserAvatar name={`${agent.firstName} ${agent.lastName}`} className="h-8 w-8"/><strong>{agent.firstName} {agent.lastName}</strong></div></TableCell><TableCell>{agent.role}</TableCell><TableCell><span className="flex items-center gap-2"><span className={`h-2 w-2 rounded-full ${agent.status==='Online'?'bg-green-500':'bg-gray-300'}`}/>{agent.status}</span></TableCell><TableCell>{formatCurrency(agent.revenue||0)}</TableCell><TableCell>{agent.hoursSpent} hrs</TableCell><TableCell>{formatDate(agent.dateAdded||new Date())}</TableCell></TableRow>)}</TableBody></Table></div>;
