'use client'; import { useForm } from 'react-hook-form'; import { z } from 'zod'; import { zodResolver } from '@hookform/resolvers/zod'; import type { Deal } from '@/types/deal.types'; import { LogFeed } from '../LogFeed'; import { Button } from '@/components/ui/button'; import { Label } from '@/components/ui/label'; import { Input } from '@/components/ui/input'; import { Textarea } from '@/components/ui/textarea'; const schema=z.object({conditions:z.string().min(5),value:z.coerce.number().positive(),followUp:z.string().min(1)}); type Values=z.infer<typeof schema>; export const SettlementLog=({deal}:{deal:Deal})=>{const {register,handleSubmit,formState:{errors}}=useForm<Values>({resolver:zodResolver(schema),mode:'onBlur'});return <div className="space-y-5"><LogFeed logs={deal.logs.filter((l)=>l.type==='SETTLEMENT')}/><form onSubmit={handleSubmit(()=>undefined)} className="space-y-3 border-t pt-4"><h4 className="font-semibold">Settlement details</h4><div><Label>Special conditions</Label><Textarea placeholder="Enter agreed conditions…" {...register('conditions')}/>{errors.conditions&&<p className="text-xs text-danger">Add a short summary</p>}</div><div className="grid grid-cols-2 gap-3"><div><Label>Contract value (GHC)</Label><Input type="number" {...register('value')}/></div><div><Label>Payment follow-up</Label><Input type="date" {...register('followUp')}/></div></div><Button className="w-full">Save log</Button></form></div>};
