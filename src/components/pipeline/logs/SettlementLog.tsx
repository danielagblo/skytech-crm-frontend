'use client';
import { useForm } from 'react-hook-form';
import { z } from 'zod';
import { zodResolver } from '@hookform/resolvers/zod';
import type { Deal, DealLog } from '@/types/deal.types';
import type { User } from '@/types/user.types';
import { useAddDealLog } from '@/hooks/useDeals';
import { LogFeed } from '../LogFeed';
import { Button } from '@/components/ui/button';
import { Label } from '@/components/ui/label';
import { Input } from '@/components/ui/input';
import { Textarea } from '@/components/ui/textarea';
const schema = z.object({ conditions: z.string().min(5, 'Add a short summary.'), value: z.coerce.number().positive('Enter a positive contract value.'), followUp: z.string().min(1, 'Choose a payment follow-up date.') });
type Values = z.infer<typeof schema>;
export const SettlementLog = ({ deal, logs, users }: { deal: Deal; logs: DealLog[]; users: User[] }) => { const mutation = useAddDealLog(); const { register, reset, handleSubmit, formState: { errors } } = useForm<Values>({ resolver: zodResolver(schema), mode: 'onBlur' }); const submit = handleSubmit((data) => mutation.mutate({ dealId: deal.id, data: { logType: 'SETTLEMENT', specialConditions: data.conditions, settlementValue: data.value, settlementFollowUp: new Date(data.followUp).toISOString(), body: data.conditions } }, { onSuccess: () => reset() })); return <div className="space-y-5"><LogFeed dealId={deal.id} logs={logs} users={users} /><form onSubmit={submit} className="space-y-3 border-t pt-4"><h4 className="font-semibold">Settlement details</h4><div><Label>Special conditions</Label><Textarea placeholder="Enter agreed conditions…" {...register('conditions')} />{errors.conditions && <p className="text-xs text-danger">{errors.conditions.message}</p>}</div><div className="grid grid-cols-2 gap-3"><div><Label>Contract value (GHC)</Label><Input type="number" step="0.01" {...register('value')} />{errors.value && <p className="text-xs text-danger">{errors.value.message}</p>}</div><div><Label>Payment follow-up</Label><Input type="datetime-local" {...register('followUp')} />{errors.followUp && <p className="text-xs text-danger">{errors.followUp.message}</p>}</div></div><Button className="w-full" disabled={mutation.isPending}>{mutation.isPending ? 'Saving…' : 'Save log'}</Button></form></div>; };
