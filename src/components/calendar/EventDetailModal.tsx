import type { CalendarEvent } from '@/types/calendar.types'; import { Clock,Link2,Users } from 'lucide-react'; import { Dialog,DialogContent,DialogHeader,DialogTitle } from '@/components/ui/dialog'; import { AssigneeStack } from '@/components/shared/AssigneeStack'; import { formatDate,formatTime } from '@/lib/utils'; export const EventDetailModal=({event,open,onOpenChange}:{event:CalendarEvent|null;open:boolean;onOpenChange:(v:boolean)=>void})=>{if(!event)return null;return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><DialogHeader><DialogTitle>{event.title}</DialogTitle></DialogHeader><div className="space-y-4"><span className="rounded-full bg-green-50 px-3 py-1 text-xs font-semibold text-green-700">{event.type}</span><p className="flex items-center gap-3 text-sm"><Clock className="h-4 w-4 text-muted-foreground"/>{formatDate(event.start)}, {formatTime(event.start)} – {formatTime(event.end)}</p><p className="flex items-center gap-3 text-sm"><Link2 className="h-4 w-4 text-muted-foreground"/>{event.linkedType}: {event.linkedName}</p><div className="flex items-center gap-3"><Users className="h-4 w-4 text-muted-foreground"/><AssigneeStack users={event.assignees}/></div></div></DialogContent></Dialog>};
