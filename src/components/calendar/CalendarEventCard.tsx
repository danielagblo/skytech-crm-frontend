import type { CalendarEvent } from '@/types/calendar.types'; import { formatTime } from '@/lib/utils'; import { AssigneeStack } from '@/components/shared/AssigneeStack'; const styles={CALL:'bg-green-100 border-green-300 text-green-900',MEETING:'bg-blue-100 border-blue-300 text-blue-900',PAYMENT:'bg-amber-100 border-amber-300 text-amber-900',TASK:'bg-violet-100 border-violet-300 text-violet-900'};export const CalendarEventCard=({event,onClick}:{event:CalendarEvent;onClick:()=>void})=><button onClick={onClick} className={`absolute left-1 right-1 z-10 overflow-hidden rounded-lg border p-2 text-left shadow-sm ${styles[event.type]}`} style={{top:`${((new Date(event.start).getHours()-8)*60+new Date(event.start).getMinutes())*1.2}px`,height:`${Math.max(52,(new Date(event.end).getTime()-new Date(event.start).getTime())/60000*1.2)}px`}}><p className="truncate text-xs font-semibold">{event.title}</p><p className="mt-0.5 text-[10px]">{formatTime(event.start)} – {formatTime(event.end)}</p><div className="mt-1 scale-75 origin-left"><AssigneeStack users={event.assignees} max={2}/></div></button>;
