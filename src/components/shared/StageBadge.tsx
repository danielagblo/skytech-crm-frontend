import type { DealStage } from '@/types/api.types'; import { cn } from '@/lib/utils'; const styles:Record<DealStage,string>={PROSPECTING:'bg-violet-50 text-violet-700',NEGOTIATION:'bg-blue-50 text-blue-700',SETTLEMENT:'bg-amber-50 text-amber-700',PAYMENT:'bg-green-50 text-green-700',CLIENT_RETENTION:'bg-pink-50 text-pink-700'}; export const StageBadge=({stage}:{stage:DealStage})=><span className={cn('rounded-full px-2 py-1 text-[11px] font-medium',styles[stage])}>{stage.toLowerCase().replace('_',' ')}</span>;
