import { Star } from 'lucide-react'; import { people } from '@/lib/mock-data'; import { formatCurrency } from '@/lib/utils'; import { UserAvatar } from '@/components/shared/UserAvatar'; import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from '@/components/ui/table'; export const ExecutivePerformanceTable=()=> <section className="surface overflow-hidden"><div className="p-5"><h3 className="font-semibold">Executive performance overview</h3><p className="mt-1 text-xs text-muted-foreground">Live sales performance across the team</p></div><Table><TableHeader><TableRow><TableHead>Executive</TableHead><TableHead>Deal closed</TableHead><TableHead>Revenue</TableHead><TableHead>Conversion rate</TableHead><TableHead>Ratings</TableHead></TableRow></TableHeader><TableBody>{people.concat(people.slice(1,3)).map((person,i)=><TableRow key={`${person.id}-${i}`}><TableCell><div className="flex items-center gap-2"><UserAvatar name={`${person.firstName} ${person.lastName}`} className="h-7 w-7"/><span className="font-medium">{person.firstName} {person.lastName}</span></div></TableCell><TableCell>{67+i*11}</TableCell><TableCell>{formatCurrency(person.revenue||0)}</TableCell><TableCell>{60+i*5}%</TableCell><TableCell><div className="flex">{Array.from({length:5},(_,s)=><Star key={s} className={`h-3.5 w-3.5 ${s<(i%3)+3?'fill-primary text-primary':'text-gray-200'}`}/>)}</div></TableCell></TableRow>)}</TableBody></Table></section>;
