'use client'; import { create } from 'zustand'; export interface AppNotification{id:string;title:string;body:string;createdAt:string;read:boolean} interface NotificationState{list:AppNotification[];unreadCount:number;add:(item:AppNotification)=>void;markAllRead:()=>void} export const useNotificationStore=create<NotificationState>((set)=>({list:[],unreadCount:0,add:(item)=>set((s)=>({list:[item,...s.list],unreadCount:s.unreadCount+1})),markAllRead:()=>set((s)=>({list:s.list.map((n)=>({...n,read:true})),unreadCount:0}))}));
